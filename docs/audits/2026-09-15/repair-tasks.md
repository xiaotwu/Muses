# 后续修复任务

来源：[2026-09-15 验收报告](README.md)。实施状态见 [分阶段记录](repairs/progress.md)。父项复选框仍代表完整验收；新增的子项分别记录已完成修复和待验收范围，避免将实现完成误写成全部验收通过。优先级按用户可见错误、数据正确性和依赖顺序排列。

> [!WARNING]
> **本清单尚未全部完成。** 下列区域是当前未完成内容的唯一汇总入口；后文保留原任务编号、历史实施批次和证据。父项未勾选可能表示仍需开发，也可能表示实现已完成但完整验收条件尚未满足，必须结合下方分类判断。

## 未完成内容（截至 2026-09-17）

最新基线：`v0.5.0` Release 构建、App Intents 元数据和 GitHub CI 打包通过；587 项／85 套件自动化测试通过。2026-09-17 已补齐 R010 的 Settings 子页与集合状态恢复、R017 的关键交互可见焦点、R018 的 Dock 队列能力与文本输入快捷键让行、R011 的授权过期状态，以及 R019 的队列保持回归。该结果只证明已覆盖的自动化与打包路径，不替代真实账号、外部硬件、辅助功能和长时实机验收。

### 尚需实现

- [ ] **R001 — 账号写入闭环**：远端歌单合并预览、冲突处理、幂等写入、服务端复读、部分失败恢复、创建／删除，以及生产保护解除前的真实账号验证仍未完成。指定测试歌单为 `PLVRppllwHcDw`，目前未执行任何远端写入。
- [ ] **R002 — 完整目录身份关联**：权威来源补全、多发行持久关联及全量真实资料库回归仍未完成。现有预览、快照、显式应用和回滚只覆盖直接证据支持的限定路径。
- [ ] **R009／R016 — 完整结构化目录**：六类完整结果、全部 renderer／详情／关联持久化、过期缓存实机、官方首页、榜单地区和真实订阅上传流仍未完成。不得以 yt-dlp 搜索或名称匹配冒充这些来源。
- [ ] **R014 — 播客闭环**：节目与单集身份、关注、分页、逐集续播、已听状态、倍速、前后跳转、按发布时间播放未听单集及不可用状态尚未实现。**Smart Shuffle 首批已经实现并完成限定验收，不再列为“未实现”。**
- [ ] **R015 — 账号内容与互动**：真实订阅写入、Shorts 完整布局、评论／回复／编辑／删除、账号隔离草稿及字幕／画质完整路径仍未完成。官方稍后观看／历史入口、悬浮视频和章节首批已经实现，不代表 R015 父项通过。

### 已实现，但尚未达到完整验收门槛

- [ ] **R006／R013／R021 — 媒体、Metal 与 EQ**：仍需 Control Center／硬件媒体键、应用与 iframe 音量完整双向回归、不可嵌入内容、可见窗口 GPU、Reduce Motion、物理输出切换听感及小时级资源观察。
- [ ] **R010／R017／R018 — 导航、全局玻璃与 macOS 交互**：仍需最终构建的 Tab／可见焦点、大库滚动恢复、真实 Dock、触觉设备、菜单栏小尺寸渲染，以及 60／120 Hz 重复性能采样。
- [x] **2026-09-17 修复子项**：Settings 子页路径可跨冷启动与前进／后退恢复；集合舞台、scrubber、队列抽屉保留可见系统焦点；集合的舞台／列表模式、焦点与选择以轻量 UUID 快照跨重启恢复；Dock 上一首／下一首按实际队列能力启用，播放快捷键在文本输入和 sheet 中让行。R010／R017／R018 父项仍因实机矩阵和精确滚动／性能证据未关闭。
- [ ] **R011／R019 — 账号状态与系统入口**：已区分 `invalid_grant` 授权过期并提供三语言重新登录状态；已覆盖深链重复打开当前暂停曲目时不改写集合队列和 Up Next。仍需真实 OAuth 登录／过期／helper 失败界面，以及 Shortcuts／Siri 发现、有效链接冷启动和无权限实机验收。
- [ ] **Settings／PlayerBar／菜单栏小窗**：仍需跨窗口应用音量联动、各类滑动条的鼠标与键盘焦点外观、popover 外部点击关闭、低版本降级和完整辅助功能组合。

### 受条件限制未测

- [ ] **R023 — 完整实机矩阵**：最低支持 macOS 14、其他系统版本、60／120 Hz 显示器、外部输出设备、媒体键、Control Center、VoiceOver、高对比度、Reduce Motion／Transparency、三语言、两小时混合播放与窗口切换仍未形成同一 Release 上的完整证据矩阵。
- [ ] **OAuth／发布条件**：公开 `v0.5.0` 未注入 OAuth 客户端，无法进行真实账号写入与登录验收；Developer ID 公证和正式分发验证尚未完成。OAuth、硬件或系统条件缺失时只能记录为受条件限制未测，不能记为验收通过。

### 已批准的范围调整（不属于待实现）

- [x] **R022**：保留 yt-dlp 与原生音频，不实现 Premium DRM 等价或通用 WebKit 扩展。
- [x] 稍后观看与远端观看历史使用官方页面入口；视频字幕和画质以官方播放器实际提供能力为准；悬浮视频窗口不称为系统原生 PiP。
- [x] Web Home 继续独立授权、隔离运行、只读，不参与播放决策或账号写入。

## 2026-09-16 已批准剩余计划实施

范围、依赖和证据见 [已批准剩余计划](repairs/approved-remaining-plan.md)。父项仍代表完整验收。

- [x] **R002/R009/R016 实现完成（资料库归属子项）**：播放解析与明确入库分离，旧行缺省仍入库，收藏加入、取消收藏保留；编辑元数据不再产生名称身份。
- [x] **R002/R009/R016 验收通过（自动化子项）**：27 项专项测试，包含非成员播放/历史、显式导入复用 UUID、收藏与取消、空值兼容和磁盘重开。尚不等于旧 schema 升级和全量资料库验收。
- [x] **R006 实现完成（队列及恢复子项）**：插播实际当前项独立持久化，集合续播位置保留，历史记实际播放项；循环单次推进；恢复独立于聆听统计，睡眠暂停、唤醒不自动播放。
- [x] **R006 验收通过（自动化子项）**：67 项媒体/队列专项测试通过；本轮完整测试 523 项通过。硬件、两小时会话和音视频渲染仍未验收。
- [x] **R009/R016 实现完成及验收通过（截断/错误回退子项）**：取消 16 条上限，真实加载更多实机通过；拒绝 yt-dlp 不可信 browse 提取，停止关键词冒充官方推荐，隔离旧 baseline 缓存。公共推荐目前明确不可用；结构化替代目录、真实分页、榜单地区仍未实现，不标为功能通过。
- [x] **R002 验收通过（归属 schema 升级/恢复基础）**：独立旧库副本升级，3 首曲目与 16 张用户数据表原有列对账一致；WAL 一致性快照与独立恢复通过。源库未写入，历史身份补全预览/应用/撤销仍未实现。
- [x] **本轮自动化回归**：529 tests / 74 suites 通过（旧库副本测试另行显式启用通过）。Release 编译通过，Debug 隔离实机检查通过；最终 Release 完整矩阵未执行。


### 2026-09-16 历史身份保护追加批次

证据：[历史身份保护与只读预览](repairs/catalog-identity-safety.md)。R002／R009／R016 父项仍未全部完成。

- [x] **R002 实现完成（启动保护子项）**：取消启动时重复视频 Track 合并、删除 UUID、重挂引用和自动补写发行身份；目录重建只更新可重建缓存，不创建或重新关联历史导入曲目。
- [x] **R016 实现完成（发行类型证据子项）**：不以标题后缀或缓存曲目数量推断 Single／EP；已有明确类型保持，缺证据的新投影为 unknown。
- [x] **R002 实现完成（只读预览计算子项）**：不可变预览按 Track UUID 输出直接导入证据，保留重复条目，多发行候选标为歧义；同视频的另一 UUID 不继承证据。
- [x] **R002 验收通过（自动化子项）**：完整 535 项测试通过，追加预览反例后专项 4 项通过；重复 UUID／笔记／歌单引用、只读预览和发行类型证据均有覆盖。
- [x] **R002 实现完成（只读预览界面）**：Settings → Account → Diagnostics → Library identity review，支持曲目标题／UUID 搜索、刷新、逐条证据展开及来源链接。英文、简中、繁中字符串齐备；隔离实例验证空态和单曲未解析态，见[界面证据](repairs/catalog-identity-review.md)。
- [x] **R002 实现完成及验收通过（显式应用／回滚子项）**：预览复核、独立快照、逐表用户数据对账、失败恢复、回滚日志及确认界面已完成；隔离磁盘夹具实机应用／回滚通过，未迁移用户真实库。
- [ ] **R002 待实现／验收**：多发行持久关联、完整来源补全及全量真实资料库验证。

### 2026-09-16 迁移与公共目录续修

证据：[迁移应用与公共目录验证](repairs/migration-and-public-catalog.md)。

- [x] **R009／R016 实现完成（公共目录搜索首批）**：独立匿名结构化 provider、来源类型与稳定 ID、真实分类参数／分页、原生专辑曲目详情、取消和失败重试；补充 yt-dlp 搜索独立标注。
- [x] **R009／R016 验收通过（限定路径）**：真实 Bruno Mars 分类／两页歌曲／专辑 browse 网络测试与隔离实机通过；全量 561 项／80 套件通过。
- [ ] **R009／R016 剩余实现**：完整 renderer／详情／关联持久化／过期缓存、官方首页／榜单／订阅流；首批路径不代表完整目录验收。
- [ ] **R014／R001／R015 与完整矩阵**：Smart Shuffle 首批已在后续批次实现并完成限定验收；播客、账号写入闭环及 R015 其余互动仍待实现。OAuth／外部硬件／其他系统版本受条件限制，长时及最终 Release 全矩阵仍未执行。

## 用户明确加入的后续任务

### 2026-09-16 界面追加修复（优先批次）

证据：[队列、设置与系统播放器](repairs/chrome-followup-validation.md)。此批次不替代下方全部剩余功能。

- [x] **R017 实现完成**：右侧全高玻璃队列、紧凑封面行与可见操作菜单；插播当前项独立显示，保留集合、Up Next、历史及原有操作。
- [x] **R017 验收通过（局部实机）**：macOS 27 浅色、真实公开搜索集合、队列全高和曲目菜单；Settings 入口与六分类统一语义图标／选中样式；音量无蓝色焦点框，粉色键盘焦点和方向键调节通过。
- [x] **R018 实现完成**：菜单栏小窗使用共享玻璃、系统字号和输出／音量控件，按内容计算尺寸；补充外部点击关闭的事件监听与关闭清理。
- [x] **R006/R018 实现完成及验收通过（自动化子项）**：系统播放信息提供封面，加载中／失败时提供完整 Logo；异步旧封面不能覆盖新曲目。完整测试 530 项通过。
- [ ] **受条件限制未测**：菜单栏小窗真实展开／外部点击关闭、输出设备切换及系统 Control Center 封面最终显示。系统菜单栏自动化读取超时；实现和编译不代表这些项目验收通过。
- [ ] **R017/R023 待验收**：本次改动的深色、三语言、最小／全屏、高对比度、降低透明度／动态效果、VoiceOver 与 60／120 Hz 完整矩阵。全应用视觉统一不能仅凭本批局部截图标为完成。

- [ ] **R001 — 生产远端歌单 Push。** 保留当前保护开关，单独完成沙盒演练、频道／所有权校验、差异预览、幂等写入、部分失败恢复，再提供生产策略。验收：授权账号的增删排序和重试准确；拒绝非所属歌单；未确认不得写入；“已同步”必须对应服务端复读结果。远端创建／删除歌单也要有完整闭环，不能只解开现有 item 写入开关。
- [ ] **R002 — 专辑／艺人稳定身份。** 消除名称作为持久身份或合并依据；使用真实 browse／channel／playlist ID。缺真实 ID 的对象明确未解析，不按名称合并。验收：同名不同艺人／专辑不合并，原稳定身份不变，收藏、顺序、历史和引用保留；迁移必须有独立验证与回滚边界。不要在纯 UI 改动中顺手迁移资料库。
  - [x] 已停止生成 artist:name／album:name／single:video 目录身份；实时目录排除历史名称身份，仅接收 browse／channel／官方专辑 playlist ID。缺失身份明确提示，歌曲保留在 Songs。名称导航遇到同名多对象时拒绝任取首项。
  - [x] 测试覆盖未解析对象不分组、原曲目 UUID／收藏／元数据／旧身份字段和缓存保留、在线导入拒绝非视频及名称身份；实机确认未解析提示。
  - [x] 直接无歧义导入证据的迁移快照／验证／显式应用／回滚及隔离数据回归已完成，见本页最新续修记录。
  - [ ] 完整权威来源补全、多发行持久关联及全量真实资料库回归仍待完成。

## 第一阶段：先消除确定的错误

最新证据：[第一阶段专项修复与验收](repairs/phase-1-validation.md)。本批完成 R025／R003／R004／R005／R007／R026／R008；R011 已完成文案与来源修正，真实账号验收仍受 OAuth 构建配置限制，按用户 2026-09-16 决定保留到待做清单，不阻塞第二阶段。

- [x] **R025 — 笔记保存与书签位置。** 完成明确的保存／取消或可靠自动保存，失败不能静默；书签操作不得覆盖草稿；新增书签取正确当前曲目的实际播放位置，非当前曲目提供明确时间选择。验证已有时间字段单位，禁止未经迁移直接更换单位。验收：编辑、Done、重开、重启、增删书签、只读开关、存储失败；笔记可搜索；书签可实际跳转。接回 BookmarksView 或提供等价入口。
  - [x] 已实现明确保存／取消、失败反馈、书签时间校验与跳转；持久化、搜索与非法时间测试通过，实机保存重开通过。
  - [x] 独立磁盘存储关闭／重开及保存失败回滚测试通过；只读进程实机确认没有编辑器、增删按钮禁用，Done 可关闭。修复了 TextEditor 禁用后仍可输入，以及 sheet 编辑空格被底层播放快捷键截获的问题。书签默认跟随实际进度，手动输入则锁定指定时间。
- [x] **R003 — 搜索结果类型。** 明确区分 video、channel、playlist；非视频不进入播放、队列或 Track 导入。处理旧搜索缓存。验收：混合结果夹具、真实 Bruno Mars 查询、无效 ID、批量导入和队列入口均不生成伪视频；频道进入频道页面。
  - [x] 已实现资源类型分类、旧缓存同规则判定、非视频导入拒绝与队列过滤；真实 Bruno Mars 查询已验证。
  - [x] 歌单批量导入先过滤非 video，再生成 Track／ImportItem；混合频道、合法视频及非法 ID 夹具通过。URL 表单与服务共用严格解析。频道打开官方频道页；原生频道详情仍属于 R009。
  - [x] 在线目录单曲和整专辑导入增加资源类型检查，拒绝频道／非法视频条目进入 Track。
- [x] **R004 — 浅色 Now Playing。** 背景与文字使用同一表面语义，保证歌词、标题、时间、transport 和音量在两种外观都可见。验收：浅／深色、浅／深封面、高对比度及降低透明度的实机截图与操作；修复不可只强制整个应用深色。
  - [x] 已修复浅色全屏背景／文字语义及迷你播放器标题；浅色实机截图见分阶段记录。
  - [x] 实机验证 Bruno 明亮封面与 Tyla 深色封面，浅／深背景和同步／非同步歌词；Bruno 在提高对比度＋降低透明度下的浅／深外观已截图。音量条修复固定白色，并增加方向键操作。
  - [x] 深色封面在提高对比度＋降低透明度下的浅／深外观通过；1000×680 窗口切换为上下排列，实机滚动可查看歌词，标题、时间、transport 与音量清晰。
- [x] **R005 — 当前曲目与正在播放。** 把当前身份、暂停、加载、错误状态分开；卡片按钮行为与标签一致，经过 PlaybackService。验收：暂停当前歌、重新播放、其他歌激活、键盘和上下文菜单一致；保留集合队列和卡片激活语义。
  - [x] 已统一当前卡片的播放／暂停图标、标签与 PlaybackService 操作；暂停状态实机验证。
  - [x] 主动作区分加载中的播放意图、暂停与错误重试；重试保持集合队列。加载／错误行为测试通过；实机右键 Play 后卡片与菜单均变为 Pause，⌘P 暂停后均恢复 Play。sheet 和文本输入不再被无修饰快捷键截获。
- [x] **R007 — 无时间歌词。** 非同步歌词不套逐行距离模糊／假高亮；原文、翻译、音译始终可读。验收：纯文本、同步文本、无歌词、AI 不可用、翻译失败及切曲取消。
  - [x] 已去除无时间歌词的距离模糊和低透明度。
  - [x] 主歌词、全屏和桌面读取活跃 transport；降低动态效果不再停止歌词时钟。纯文本显示无时间轴提示，缓存可升级为文字和录音匹配的供应商同步版本。
  - [x] Tyla — IS IT LOVE 的真实同步歌词已验证跳转、播放跟随、当前行强调及全屏共享时间轴。Risk It All 仍为纯文本，候选版本不一致时没有替换或伪造时间。
  - [x] 主／全屏／极简复用同一歌词视图；去掉当前行固定白描边。切曲原歌词清空，翻译只使用匹配目标语言的版本，关闭翻译不再加载缓存的其他语言。新增自动请求不能覆盖手动选择、取消请求不能发布结果的竞态测试。
  - [x] Debug 进程故障注入实测 AI 不可用及 Translation 失败：提示可由 AX 读取，原文保持显示；正式构建不包含注入分支。本机真实 AI 可用；跨设备／地区验证仍归 R023。
- [x] **R026 — 桌面歌词降级。** 区分同步歌词、纯文本和真正无歌词；纯文本可阅读或准确提示当前只支持同步滚动，不显示误导性“无歌词”。统一切曲取消、手动匹配、时间偏移及来源；回归主歌词、抽屉、全屏与桌面表面。
  - [x] 已为非同步桌面歌词增加阅读提示，并区分加载、无歌词、同步前奏等待；时间偏移与四种状态测试通过。
  - [x] 新表面读取共享时间偏移，不再用旧 TrackSnapshot 覆盖用户刚调整的值；优先重新读取持久化偏移，相关测试通过。桌面层监听来源、智能匹配和手动选择修订。
  - [x] 通过真实桌面截图验证纯文本提示、同步当前行、长句两行及来源切换后的稳定显示；固定浮层高度并加深底色，设置其他桌面选项不再重建歌词。
  - [x] 补回时间调整图标入口（±0.5 秒、重置）；实机验证保存和重置。首页直接播放的未入库曲目按 YouTube 视频 ID 保存偏移，不制造资料库条目；重启、重新生成快照、后续入库和重置测试通过。
  - [x] 主歌词、全屏与桌面采用共享偏移与当前行规则；时间边界和跨表面偏移由测试覆盖，主／全屏／桌面真实同步歌词与纯文本降级分别实测通过。未将控件截图描述为多窗口同帧测量。
- [x] **R008 — 隐藏表格辅助功能。** 修复卡片与表格切换后的实际 AppKit AX 子树，不靠仅检查 SwiftUI modifier。验收：屏幕上只有当前模式；AX／VoiceOver／Tab 不进入隐藏行；返回时保留排序、选择和滚动。
  - [x] 已通过原生 NSHostingView 隐藏非活跃表面；实机 AX 树不再暴露隐藏表格。
  - [x] 实机 VoiceOver 开启后切换卡片／表格、VO 导航及 Tab 通过，隐藏 outline 不出现在 AX 树；返回保留倒序、选中 Tyla 行与水平滚动位置。补修延迟求值导致卡片返回后长期禁用的问题，重测启用状态正常。
- [ ] **R011 — 账号说明准确性（用户明确延期）。** 修正 Chrome 分支和设置路径；OAuth、播放 Cookie、Web Home 状态分别准确且有直接恢复入口。验收：各 Cookie 来源、未登录、登录、过期和 helper 失败；三语言都一致。
- [x] 已修正 Chrome 来源说明和设置路径，相关测试通过。
- [x] 实机验证 None／Safari／Chrome／Firefox／Cookie File 五种来源说明；英语／简中／繁中即时切换及未配置登录的说明一致。所有临时偏好已恢复。
  - [x] OAuth 刷新端点的 `invalid_grant` 不再被当作普通网络／交换错误；会清除失效会话、进入可恢复的“授权过期”状态，并提供英语／简中／繁中重新登录文案；会话与账号服务测试通过。
  - [ ] 当前构建未配置 OAuth，无法验收真实登录、令牌过期及已连接账号 helper 失败的三语言 UI；现有 helper 失败、身份隔离与降级测试通过，不能替代真实账号验收。

## 第二阶段：统一媒体状态与图形基础

2026-09-16 续修：[第二至第四阶段续修记录](repairs/phase-2-through-4.md)。补修视频音量版本隔离与初始化静音状态；本轮不把既有硬件／长时待验收项勾选。

2026-09-16 更新：[第二阶段实施与验收记录](repairs/phase-2-validation.md)。R012、R020 完成；R006、R013、R021 已补齐下述代码修复与自动化回归，但仍保留未取得证据的实机／跨环境验收，第二阶段没有误标为全部通过。按用户 2026-09-16 决定，R006／R013／R021 剩余硬件媒体控制、完整双向视频音量、GPU 窗口显示、Reduce Motion、设备听感及长时验证继续保留待办，不阻塞第三阶段。

- [ ] **R006 — 音视频会话协调。** 在现有 PlaybackService 边界内明确当前活跃媒体，连接视频进度、暂停、音量、错误、结束及系统命令；保留原生音频引擎和 Web Home 隔离。验收：1:04 打开／返回、播放或暂停时切换、快速切曲／关闭、视频不可嵌入、Control Center 和媒体键；永不双声源，不把旧视频状态写回新歌曲。
  - [x] 已实现入口状态快照：原生先暂停、视频按原进度／播放意图／音量初始化；快速切曲关闭旧表面。暂停打开和关闭保持暂停已实机验证。
  - [x] 已接入独立视频会话、进度／播放状态回传、应用和系统命令路由、错误／结束事件；关闭等待 WebKit 暂停确认后归还原生音频。实机视频接续、暂停、返回进度及 ⌘P 控制通过。
  - [x] 已接入 iframe 音量／静音回传，写回同一音量偏好及原生引擎；拒绝过期会话和无效值，不回声发送命令。相关测试通过。
  - [x] 加载期间 seek／play／pause／volume 与 200 次重开关闭测试通过；迟到的 iframe 播放包不能撤销更新的系统暂停／播放意图。macOS Now Playing 额外发布 playbackState。
  - [x] 音量／静音记忆集中到 PlaybackService，保留低于 5% 的音量并跨重启恢复，避免取消静音突跳到 70%／80%；播放器、视频回传和全局快捷键共用。低音量保存与视频静音测试通过。
  - [x] 实机视频由 3:32 接续，片尾自动关闭并进入集合下一首；另一次视频静音返回原生为 0%，应用菜单暂停后返回仍暂停并带回 0:46 进度。
  - [x] 音量命令带版本号，旧 iframe 音量回包不能覆盖更新的应用设置；初始化明确同步静音／取消静音，JS 等待短时音量应用后再报告。新增旧版本回包测试通过。
  - [x] 跳转命令带版本号；旧进度不能撤销新 seek，旧播放结束包不能误推进集合。JS 对跳转应用有短时等待，回归覆盖旧进度、旧结束包及新进度确认。
  - [ ] Control Center／硬件媒体键实机、应用→iframe 音量的完整双向回归、不同内容的不可嵌入失败与长会话验收尚未完成。
- [x] **R012 — 图形设置真正接线。** 使用实际页面的统一渲染能力／质量策略，校正 GPU 开关名称和范围；Apple Silicon 默认使用可用的硬件路径。验收：切换会改变目标渲染器，UI 不承诺未实现的全局加速；不普遍增加离屏光栅化。
  - [x] 实际频谱入口已使用 GPU 设置，Metal 不可用时回退统一 SpectrumView。
  - [x] 设置名称改为 Metal 频谱渲染，明确仅影响音频信息中的频谱，不冒充全局 GPU 开关。
  - [x] 设置界面实际切换 1→0→1；关闭时 Canvas 频谱可见，开启时创建 Metal renderer，日志确认旧采样归属被移除。设置明确只影响 Audio Info 频谱。真实 Metal GPU 离屏像素回读通过；窗口内最终显示证据的限制记在 R013。
- [ ] **R013 — Metal 生命周期。** 正确 static dismantleNSView，由 coordinator 持有清理所需依赖；去除离场 handler、释放资源、设备／pipeline 失败回退。验收：重复打开关闭频谱、切曲、暂停、Reduce Motion、编译失败；观察 handler 数量、内存和帧循环回落，而不只单测 shader 常量。
  - [x] 已实现 static dismantle、资源释放和 handler 所有权；旧 owner 不移除新 handler 的测试通过。
  - [x] 暂停／Reduce Motion／无分析能力时停止 Metal 帧循环和采样；Canvas 使用仅保留最新帧的线程安全缓冲。Metal 使用命令自有常量，释放 pipeline／queue，并阻止离场的失败回调切换新表面。修正实数 FFT 的奇偶打包和补零。
  - [x] 20 次 dismantle 弱引用释放、无设备／无效 shader、真实 GPU 像素回读及 10,000 帧有界缓冲测试通过。实机连续 6 次打开／关闭均恢复 handler=0，renderer 记录停止；暂停闲置 CPU 采样回到 0.2%。
  - [ ] 工具捕获中 Metal 为空、WebKit 视频也为黑块，无法据此证明窗口内渲染失败或成功；仍需可见窗口的最终核验。Reduce Motion 系统开关实测、小时级内存／能耗和跨设备验证尚未完成；短期 RSS 数据不能替代长时泄漏验收。
- [x] **R020 — 运行能力与跟踪。** 能力状态区分系统 API 可用和当前操作可用；PerfTrace 使用有界缓存。验收：不可分析的流阶段不显示全支持，权限或设备失败可解释；长时间事件数量有上限。
  - [x] PerfTrace 已有界；音频分析能力随实际引擎阶段变化，相关测试通过。
  - [x] 全局快捷键能力依据 Carbon 注册结果和启用状态，冲突显示恢复说明；修复 EventHotKeyID 误读 signature 的错误并测试真实 Carbon 事件结构。输出设备能力依据枚举／读取／写后复读结果，失败不更新已选设备。媒体键维持 limited，托盘／窗口等字段仅表达平台 API 支持，不表示每次操作成功。
  - [x] 音频错误不再报告分析可用；PerfTrace 超容量淘汰、设备写入失败、空复读及事件身份验证通过。设备刷新不再自动改动系统默认输出，输入设备不进入输出列表，UI 明示用户选设备的系统范围。
- [ ] **R021 — EQ 能力边界。** 明确流式开始与缓存引擎切换时 EQ 是否生效，保证设置状态不冒充实际处理。评估需要的参数均衡功能；不为对齐而直接改变整个系统输出。验收：预设、旁路、切阶段、切设备、重播时听感／处理参数连续且 UI 准确。
  - [x] 缓存切换后重施 EQ 和频谱意图，UI 显示实际处理能力；引擎切换参数测试通过。
  - [x] 手动 EQ 参数、旁路状态在启动时恢复，编辑器重开不重置未另存的调整；改动后预设标为 Custom。旁路提交空滤波列表，并保留参数供恢复；播放阶段切换仍复用请求参数。
  - [x] 实机 Bass Boost 数值、Flat 恢复、旁路提示通过；新增保存／重建 facade／取消旁路测试，频率、增益、带宽按 AVAudioUnitEQ 合法范围处理。编辑控件补充频率和 dB 辅助功能标签。
  - [x] 参数 EQ 范围评估：保留十段图形编辑器及自定义预设；底层使用 parametric filter。历史字段 q 实际为八度带宽，已明确注释，不把历史数据静默解释成共振 Q，不扩展为另一套音频引擎。
  - [ ] 物理输出设备切换／重播的人工听感连续性与实际 DSP 参数回归仍需补齐；本批没有用 UI 曲线或测试断言代替听感结论。

## 第三阶段：全局 Liquid Glass 与动画质量

2026-09-16 更新：[第三阶段实施与验收记录](repairs/phase-3-validation.md)。已修复集合界面状态丢失、命令可用性检查、歌词选项布局，并补充卡片一次性激活动效；用户已退出旧进程，新构建已部署。File 主窗口命令与搜索目的地可重建主窗口。按用户决定，R017／R010／R018 剩余内容保留待办，继续第四阶段，不将第三阶段标为全部完成。

- [ ] **R017 — 原生玻璃和动效完成度。** 复用语义 GlassSurface；为相邻 chrome 控件组织容器、交互响应与形变；完善侧栏、PlayerBar、队列、设置、封面连续性。实现当前合同缺失的卡片激活效果，保留原有视觉结构。验收：实际渲染的高低对比封面、浅深外观、不同窗口、鼠标／键盘／触控板；Reduce Motion 无持续装饰动画；60／120 Hz 对应预算下做重复采样。先记录基线再宣称优化，不以“更多 blur／shadow”为完成标准。
  - [x] 已按最新截图取消视频外层边框／标题，保留关闭与 Escape；移除侧栏顶部 Logo／字标，设置入口和菜单栏使用 Logo，常用表面动作图标化。
  - [x] 玻璃 primitive 动态响应高对比度；Now Playing 局部原生玻璃分组，修复歌词选项 Menu 扩宽错位。新增单次 0.52 秒卡片燃烧边缘效果，快速重触发／离场／降低动态效果会取消，不延迟播放。
  - [x] 实机浅／深外观与窗口内布局检查；系统 Reduce Motion 真实切换 0→1→0，卡片播放与队列操作保持可用。跨整窗玻璃容器实验导致文字遮挡，已撤回并验证恢复。
  - [ ] 燃烧过程逐帧证据、完整输入矩阵、玻璃连续性及 60／120 Hz 性能预算仍按原门槛验收，不宣称全局性能提升。
  - [x] 视频按可用宽高居中；最新关闭方式为顶部中央触发区悬停弹出，Tab 及 Escape 可用；完整原始 Logo 用于 AppIcon，菜单栏改为独立简化竖琴模板。
  - [ ] macOS 系统设置图标缓存和菜单栏小尺寸渲染仍需实机确认。
  - [x] 集合舞台、scrubber 和队列抽屉移除禁用系统焦点环的覆盖，Tab／键盘操作保留可见焦点；Reduce Motion 仍取消装饰性动效。
- [ ] **R010 — 全局导航一致性。** 新目的地进入原生应用菜单、焦点和可访问导航；准确反映当前选择。验收：侧栏展开／折叠、菜单导航、返回和语言即时切换不丢页面状态。
  - [x] 喜欢、音乐视频和订阅目的地已接入原生应用菜单。
  - [x] 原生统一工具栏按最新截图同排放置红绿灯、侧栏折叠、后退和前进；窗口级历史保存对象身份，支持分支和删除后的概览回退。实机返回／前进／折叠及 ⌘[ 通过。
  - [x] 集合模式、卡片焦点、表格排序／选择／列定制按窗口内稳定路由保存，移除翻译标题变化时的模式重置。实机第二张卡片返回恢复；倒序 Tyla 选择在 Home→Back 和英／简／繁切换后保持。
  - [x] 最后页面按版本化轻量路由保存，重启只恢复当前页，不恢复整个导航栈；对象不存在时回所属列表。订阅页按账号 ID 核对，账号切换／断开清空私有导航。7 项专项测试、541 项完整测试和独立最终 Release 冷启动／缺失歌单回退通过，见[导航恢复](repairs/navigation-restoration-closeout.md)。
  - [x] Settings 子页路径写入版本化路由快照；冷启动与前进／后退恢复 `SettingsDestination` 路径，旧快照无路径字段时安全回退到分类首页。
  - [x] Songs／喜欢／音乐视频、歌单／导入和发行详情的集合模式、卡片焦点与表格选择以最多 500 个 UUID 的有界快照跨重启恢复；不持久化模型对象、翻译标题、播放状态或排序规则。
  - [ ] 最终构建 Tab／大库跨目的地滚动位置回归；精确表格滚动偏移尚未补齐；真实账号切换验收受 OAuth 条件限制。
- [ ] **R018 — macOS 交互补齐。** Dock 播放菜单、缺失快捷键和适度触觉反馈；菜单启用状态依赖真实播放能力。验收：窗口关闭时 Dock 与媒体状态正确；无 Force Touch 设备不影响操作；快捷键无冲突，不抢占文本编辑。
  - [x] Dock 菜单已通过 PlaybackService 提供播放／暂停、上一首、下一首。
  - [x] 命令执行和菜单启用接入真实媒体可用性；补充歌词／视频快捷键及系统指针激活触觉反馈。
  - [x] 修复已关闭主窗口没有创建路径：稳定 scene ID、File 主窗口命令、Dock reopen 与菜单栏／全局快捷键共用创建入口；构建通过。
  - [x] 用户已退出旧进程；实测 File → Main Window 重建主窗口，搜索窗口 → Songs 在主窗口关闭后正确恢复目的地。
  - [x] 主窗口身份增加弱引用登记，SwiftUI 重写标识／恢复名称后仍可正确识别；测试覆盖已登记主窗口、搜索及迷你窗口隔离。
  - [x] Playback 菜单快捷键在 `NSTextView` 或已展示 sheet 时禁用，避免 ⌘←／⌘→ 等操作抢占文本编辑；纯策略测试覆盖正常、文本输入和 sheet 三种状态。
  - [x] Dock 上一首／下一首的菜单启用状态改为读取实际队列能力；无当前队列、队列边界和存在前后项均有自动化覆盖。
  - [ ] 真实 Dock、文本快捷键冲突与触觉设备仍待验收。CUA 普通 launch_app 不能代替 Dock reopen 测试。
  - [x] 歌词匹配／时间调整弹出层向 Now Playing 传递交互状态，避免底层 Escape／空格抢占。

## 第四阶段：其余 Kaset 功能差距

2026-09-16 开始执行：[设置整合与第四阶段首批记录](repairs/phase-4-validation.md)。本阶段尚未全部完成。

- [x] Settings 集成主窗口，保留侧栏、PlayerBar 和返回导航；分类改为语义图标和简短标签，完整标题保留。常规／最小窗口及英／简／繁切换实测通过。
- [x] Settings 分类区、表单和播放器后方统一使用主内容背景，移除系统滚动容器重复底色；浅／深实机截图通过。
- [x] 修复设置标题挤入系统工具栏：主分类及子页标题在内容区显示，子页返回独立于全局返回；Settings 文字入口覆盖展开／折叠侧栏。可用宽度不足 640pt 时分类变为菜单，避免队列打开后表单过窄。
- [ ] 设置全部子页、VoiceOver／Tab、辅助功能外观及无主窗口时快捷键回归。

- [ ] **R009 — 完整搜索与失败状态。** 视频、歌曲、专辑、艺人、歌单、播客使用类型化结果；准确标注来源；支持加载、取消、错误、重试、空状态和分页。依赖 R003／R002，禁止用名称生成稳定目录身份。
  - [x] 新查询立即移除旧结果；取消后的晚到响应失效；统一重试任务；关窗取消搜索；服务缺失及取消均有明确反馈。3 项行为测试及真实搜索／取消通过。
  - [x] 搜索目标暂存到主窗口创建后消费；实测关闭主窗口后 Search → Songs 到达正确页面。
  - [x] 增加加载更多：20 条递增请求，保留已显示顺序并按视频／资源 ID 去重，失败保留现有结果；取消后排队的旧请求不能复活旧查询。测试覆盖排名变化、去重与取消竞态。后端使用扩展排名前缀，不伪称服务器 continuation。
  - [ ] 六类完整结果、类型筛选、远端目录原生详情，以及真实多页网络失败／重试验收仍未完成。
- [ ] **R014 — Smart Shuffle 与播客。** 两个子项独立记录，父项仅在两者都达到验收门槛后关闭。
  - [x] Smart Shuffle 首批实现及限定验收：推荐注入尊重 Up Next／历史／单曲循环，不污染歌单；推荐来源、间隔及队列恢复已有自动化与短时实机证据。完整长时音频矩阵仍归 R023。
  - [ ] 播客闭环：节目、关注、单集分页、逐集续播、已听状态、倍速、前后跳转、按发布时间播放未听单集，以及离线／不可用提示仍待实现与验收。
- [ ] **R015 — YouTube 内容与互动。** 分开交付：订阅流与订阅操作、Shorts、稍后观看、远端历史、评论／回复／互动、原生字幕与画质、章节、PiP。每项明确加载／分页／失败／账户切换和集合播放；远端变更有实际确认与失败恢复。不得把本地收藏或导入历史假装成远端用户状态。
  - [x] **实现完成（批准的悬浮视频范围）**：会话拥有唯一 WebView，覆盖层与独立悬浮窗口只交换承载；提供返回、播放／暂停、关闭及 Escape。关闭等待所有视频媒体停止确认后返回原生音频，不称系统原生 PiP。
  - [x] **实现完成（批准的官方页面范围）**：Settings → Account 提供稍后观看及 YouTube 观看历史的官方页面链接，明确使用浏览器登录账号；不读取 Cookie，不冒充本地历史或远端数据同步。Debug 构建通过；登录后的实际私有页面受账号条件限制未测。
  - [x] **验收通过（局部实机与自动化）**：Bruno Mars 真实视频覆盖层→小窗→覆盖层→小窗，进度连续；暂停后 Escape 返回音频仍暂停于 0:34。新增旧 host 回包／拆卸隔离、重复关闭及停止确认测试；532 tests / 75 suites 与 Release 编译通过。证据见[悬浮窗口记录](repairs/floating-video-validation.md)。
  - [ ] **待验收**：跨屏／最小窗口／全屏组合、关闭主窗口后返回、快速连续操作长会话、硬件输出切换及音源实测。上述局部结果不等于 R015 全部通过。
  - [x] 章节首批：Now Playing 更多菜单提供原生章节弹层，按需读取 yt-dlp 真实章节，校验视频身份／时间、去重排序；加载、空状态、失败与重试分开，切曲／关闭取消请求，跳转复用 PlaybackService。解析测试及真实无章节内容的空状态通过。
  - [x] 视频覆盖层补充按需显示的章节入口，关闭按钮仍位于顶部中央；章节跳转复用 PlaybackService，Tab 可进入章节按钮，Escape 先关闭章节弹层。
  - [ ] 有章节长视频的实际跳转、网络失败／重试和多语言辅助功能仍待验收；其他互动子项仍未完成。
- [ ] **R016 — 发现与目录。** 核对推荐、发行、榜单、类型的真实数据来源和刷新；公共发现与个性化页面明确区分。验收：稳定身份、过期缓存、空／不可用和 collection-context 播放；Web Home 仍只读且保留独立授权边界。
  - [x] 艺人在线结果仅接收相同 channelID；不把按名字搜到的视频或普通用户歌单当作官方专辑，也不凭标题推断 Single／EP。专辑曲目限定已解析发行 URL，过滤非视频并去重。
  - [x] 在线目录缓存改为 15 分钟、最多 100 项；手动加载绕过缓存，网络／身份失败不伪装为空目录，艺人错误有重试入口。身份隔离与强制刷新测试通过。
  - [ ] 全量远端发行／榜单／类型、真实源分页及缓存过期实机回归仍未完成。
- [ ] **R019 — URL 和自动化。** `muses://play?v=`、应用收到的 YouTube watch／短链接、外部脚本或 App Intents 均进入同一命令／播放入口。验收：无效 URL、未入库曲目、重复打开、启动中路由、队列上下文和无权限场景；不要把内部 CommandRegistry 当作外部接口。
  - [x] app-lifetime ExternalPlaybackRouter 处理本地 UUID、muses 视频 ID 和官方 watch／短链接／Shorts；新视频复用导入服务，播放只经 PlaybackService。同曲目不重建队列；待处理重复请求合并，旧请求不能抢占新播放，失败明确提示。解析和异步竞态测试通过。
  - [x] 拖入受支持的单个视频／歌单 URL 时预填导入表单，其他 URL 不再被错误吞掉。
  - [x] 无主窗口时投递 muses://play?v=invalid，窗口重建并显示不支持链接提示，未开始播放。
  - [x] macOS 15+ 新增 Play YouTube Link App Intent，经已验证 muses URL 交给同一 PlaybackService 路由；提供 AppShortcutsProvider，构建生成并打包 Metadata.appintents，Debug／Release 提取均通过。
  - [x] 当前暂停曲目被深链重复打开时只恢复播放，不重建集合队列、不清空 Up Next，不改写当前项；已入库队列内其他曲目也保留集合上下文。
  - [ ] Shortcuts／Siri 实际发现与运行、有效 URL 冷启动和无权限实机验收。当前不以编译或自动化回归通过冒充系统入口验收。
- [ ] **R022 — 架构差异评估。** Premium DRM 与 WebKit 扩展分别验证技术可行性和产品范围。当前原生音频约束下不承诺等价；需要改变架构时单独给出具体方案。通过门槛是真实受保护内容及扩展行为测试，不是显示 Premium 或扩展名称。

  - [x] 已核查 Kaset 扩展 ADR 与 Apple API，形成[具体架构比较](repairs/phase-4-architecture.md)，明确与原生音频和 Web Home 边界的冲突。
  - [x] 用户已选择保留原生音频，不实现 Premium DRM 等价或通用 WebKit 扩展。这是批准的范围调整，原 Premium/扩展功能不记为验收通过。

## 最终回归与清理

- [ ] **R023 — 跨环境验收。** macOS 最低支持版本、15 翻译、26+ Intelligence／Glass；模型未下载／设备不支持／地区限制；三语言动态字符串；账号、通知、窗口关闭后台播放、退出与恢复；键盘、VoiceOver、对比度、降低透明度／动效；大库及长会话。分别记录通过、失败和未测，不以 464 个已有测试代替这些项目。
- [x] **R024 — 退役代码及文案。** 查清旧 YouTubeMusicView／VideoWell／AboutView／WaveformView／SpectrumView 的全部引用后移除或收敛；保留正常加载占位。同步过时注释（例如 Metal 频谱注释称 60 FPS、实际设 30）和帮助入口。验收：无第二套运行中的库／播放器／搜索服务，不恢复本地文件和双模式 UI。
  - [x] 确认 YouTubeVideoWell 无引用后移除；Logo 读取覆盖 SwiftPM Resources 子目录并集中缓存。
  - [x] 已查证并移除无入口的 YouTubeMusicView、旧 YouTubeSearchView／YouTubeImportsView 子树、AboutView、WaveformView 及仅验证旧标签数量的测试。保留现用 GlobalSearch、AboutSettingsView、导入服务和 SpectrumView；Metal 注释已与 30 FPS 一致。
  - [x] 发布打包补拷 SwiftPM 资源包，避免安装后依赖开发目录；使用 SwiftPM 返回的实际 release 路径。打包合同测试已更新。
  - [x] 脱离源码目录的 Release 打包、严格 app/helper 签名、SwiftPM 字体与本地化资源、随包 yt-dlp、内置帮助和原生 Help 网站入口已验证；文档过时能力承诺与设置路径已修正。证据见[独立发布包检查](repairs/release-closeout.md)。Developer ID 公证、最低系统与最终长时矩阵仍属于 R023，本项不代表正式发布通过。

最终通过条件：功能矩阵逐项拥有可复现证据，失败项修复，未实现项完成或有明确的新范围决定；媒体、身份、权限与导航统一；截图和真实操作通过；性能结果来自相同条件的重复测量。

## 2026-09-16 续修：目录缓存、Smart Shuffle 与圆角队列（进行中）

- [x] **界面实现完成／局部实机验收通过**：右侧队列复用左侧栏圆角尺寸，采用镜像玻璃边界，保留全高集成布局；见 `repairs/smart-rounded.jpg` 和 `smart-rounded-ax.txt`。
- [x] **R009／R016 实现完成（匿名目录缓存子项）**：规范化公开页持久缓存，地区／查询／类别隔离，失败显示原时间及过期提示；游标、筛选参数和原始响应不落盘，下一页失败不以首屏替代。关联 carousel 进入独立关联区，不混入主集合。
- [x] **R014 实现完成（Smart Shuffle 首批）**：默认关闭；三首实际听过的集合歌曲后最多一首官方 Mix 候选，手动 Up Next 优先，单曲循环暂停插入，重复与迟到候选被拒绝；关闭保留当前项和显式加入条目；推荐、来源、间隔和集合游标持久化，旧队列默认关闭。
- [x] **R014 验收通过（限定范围）**：7 项规则／恢复专项及真实推荐 API 验证通过；隔离 Debug 实机连续三首后插入 Grenade，关闭后继续当前项，下一首回到 That's What I Like，Songs 保持空。见 `smart-inserted-ax.txt`、`smart-disabled-current-ax.txt`、`smart-return-collection-ax.txt`。该检查为静音短时状态检查，不等同完整音频／两小时矩阵。
- [x] **回归阶段记录**：Debug 最终 574 项／83 套件通过（含真实目录 API）；新建旧库副本升级 4 项通过；Release 编译通过，Release 全量 573 项／83 套件通过（Debug 专属 GPU 读回不纳入 Release）。
- [ ] **完整范围仍未完成**：目录全部详情／多发行用户身份关联、播客闭环、账号写入和完整矩阵仍在执行，不因上述子项勾选而将父项标为通过。
- **账号验证条件**：用户可提供测试账号，指定测试歌单 `PLVRppllwHcDw`；目前找到的构建均未注入 OAuth 客户端，已请求原配置项目名／路径。未对歌单执行写入，生产保护未解除。

本批详细实现、初次失败及修复复验、证据路径和剩余边界见[目录与推荐队列续修记录](repairs/catalog-smart-closeout.md)。

## 2026-09-16 用户新视觉决定：黑白高亮与播放界面

- [x] **实现完成**：窗口返回、歌词菜单合并、Now Playing 扩大显示范围、共享刻度音量与尾部设备选择、菜单栏去除叠加外框及重排、全局自适应黑白高亮、封面取色当前歌词。明确替代旧粉红／禁止选中光晕约定。
- [x] **局部验收通过**：574 项测试；窗口返回真实操作及浅色／深色局部渲染。
- [ ] **受条件限制未测／待操作验收**：菜单栏状态项自动化访问超时，popover 实际渲染与跨窗口同步操作、外部设备及完整辅助功能组合仍待验证。
- 详细范围与证据见[黑白高亮与播放界面重排](repairs/monochrome-now-playing.md)。原目录、播客和账号功能未完成项仍保留。

## 2026-09-16 Settings 左侧栏、歌词行距与音量点击续修

- [x] **实现完成**：歌词段间距由沉浸模式 28 → 42、其他模式 10 → 20；Settings 六分类替换音乐左栏，主页面去掉第二导航与分隔线，开放式分区与宽度限制；提供返回音乐及折叠分类图标。
- [x] **实现完成**：移除音量组件主动绘制的整圈焦点框；点击／拖动按实际刻度全宽映射，不再减去旧旋钮宽度。所有修改仍调用应用 PlaybackService.setVolume，未加入系统音量写入。保留键盘和辅助功能增减；原生进度／EQ 滑动条关闭默认 SwiftUI focus effect。
- [x] **验收通过（限定范围）**：71 项专项测试；全宽映射验证 0／25／50／100%、越界和无效宽度。隔离实机 Settings 导航替换与开放式排版通过，见 `repairs/settings-sidebar-open-sections.jpg`。
- [x] **小待办实现及局部验收**：Settings 不再按主区域宽度替换 NavigationStack，折叠侧栏后 Desktop Integration 子页保持；见 `repairs/settings-sidebar-continuity.txt`。这是 R017 宽度切换连续性子项，不代表全部草稿／焦点矩阵通过。
- [ ] **仍未完整验收**：实际菜单栏跨窗口音量联动、各类原生 slider 的鼠标／键盘焦点外观和全部辅助功能组合。为了节约本轮用量未重复全量／Release／长时矩阵；没有将此项虚标通过。

## 2026-09-16 未播放状态完整播放条

- [x] **实现完成／局部实机验收通过**：空闲与播放使用相同控件布局，封面位置使用菜单栏琴形模板 Logo 和圆角中性底色。无曲目时运输／歌词／视频禁用，队列和应用音量仍可用，时间使用破折号。隔离实机已检查渲染、按钮禁用及音量浮层入口；证据 `repairs/idle-player-complete.jpg`、`idle-player-complete-ax.txt`。
- [x] **小遗留修复实现完成**：切歌清除 PlayerBar 旧拖动状态，拖动结束核对曲目身份，防止旧手势定位新曲目；浅色进度指示点从固定白改为自适应前景色。快速切曲拖动的完整实机组合尚未测。
- [x] **专项验收通过**：71 项／4 套件通过，日志 `/tmp/muses-idle-final-tests.log`。本轮未重复全量和 Release 矩阵。

## 2026-09-16 统一窗口返回／前进

- [x] **实现完成**：原生工具栏接管 Settings 分类及多层子页、歌单／专辑／艺人／频道详情返回；移除内容区重复返回。搜索窗口补原生前进／后退与目录详情历史。
- [x] **R017／R009 简单遗留实现完成**：冷启动详情补入父列表，Settings 可返回 Home；目录前进、分支与新查询清理历史。不会恢复上次完整历史栈。
- [x] **验收通过（限定范围）**：81 项专项测试及隔离应用构建；Settings 两层返回／前进、分类切换、搜索窗口工具栏实机验证通过。
- [ ] **受条件限制未测**：真实账号频道／真实目录窗口完整导航、完整辅助功能和系统矩阵。详情见[统一窗口导航记录](repairs/unified-navigation.md)，父任务保留原状态。

## 2026-09-16 Settings 扁平分类与 Liquid Glass

- [x] **实现完成**：依最新决定取消 Settings 子页导航，右侧按区直接展开；诊断、资料库核对、帮助提升为一级分类。替代此前六分类和子页导航的视觉约定。
- [x] **实现完成**：原生玻璃按钮、玻璃选中项与选择过渡、系统开关／选项菜单；保留平台和辅助功能降级。
- [x] **验收通过（局部）**：74 项专项、最终 Debug 构建；隔离实机深浅色、主题点击切换、展开布局和选项菜单入口通过。
- [ ] **受条件限制未测**：低版本实机、完整辅助功能／右键菜单矩阵；见[扁平设置与玻璃交互记录](repairs/flat-settings-glass.md)。
