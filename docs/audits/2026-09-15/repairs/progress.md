# 分阶段修复记录

本文件记录实施进度；原验收报告保留发现当时的事实。未列为完成的工作继续按 repair-tasks.md 执行，不把代码落地等同于跨环境全部验收。

## 第十四批：R010／R011／R018／R019 状态与回归补修（2026-09-17）

- R010：集合页的舞台／列表模式、卡片焦点和表格选择改为有界的身份快照，可跨应用重启恢复；不保存 SwiftData 对象或排序真值。
- R011：Google 刷新令牌返回 `invalid_grant` 时，OAuth 会话和账号服务现在进入明确的 expired 状态、清除失效令牌，并显示三语言重新登录路径。
- R018：Playback 快捷键在文本编辑器或 sheet 接管输入时让行，避免与 macOS 文本导航键冲突。
- R019：新增当前暂停曲目深链重复打开的队列回归，验证恢复播放不会改写集合项、Up Next 或当前位置。

验证：新增与扩展的 OAuth、集合恢复、键盘作用域和外部播放专项 **35 tests / 4 suites 通过**；完整 **587 tests / 85 suites 通过**，`git diff --check` 通过。真实 OAuth／helper、Shortcuts／Siri、有效链接冷启动、Dock／触觉设备、精确大库滚动偏移及 60／120 Hz 性能仍保留为外部验收门槛。

## 第十三批：R010／R017／R018 导航与 macOS 交互修复（2026-09-17）

- R010：`BrowseRouteSnapshot` 现在保存 Settings 子页路径；旧快照通过 `decodeIfPresent` 兼容，`RootView` 生成 Settings 路由时不再丢失 `settingsPath`。
- R017：集合舞台、scrubber 和队列抽屉恢复系统默认可见焦点，不改变浏览卡片的玻璃层级；Reduce Motion 仍取消装饰性动效。
- R018：Dock 的上一首／下一首启用状态改由 `PlaybackService.canGoPrevious/canGoNext` 根据实际队列计算，避免在队列边界显示可用但无动作的菜单项。

验证：导航／快照、动效／焦点、Dock 能力专项共 **18 tests / 3 suites 通过**，`git diff --check` 通过。真实 Dock reopen、VoiceOver／Tab 全矩阵、60／120 Hz 性能、触觉设备及大库精确滚动偏移仍按原验收门槛保留未测。

## 最新：第二阶段实施与剩余验收（2026-09-16）

详见 [第二阶段记录](phase-2-validation.md)。R011 按用户决定延期；R012、R020 完成。R006／R013／R021 已补齐音视频竞态、统一静音、Metal 生命周期、FFT 和 EQ 恢复等修复，506 项测试通过，构建、签名和实机基础操作通过。硬件媒体键、完整视频音量回读、最终 GPU 窗口显示、Reduce Motion、物理设备听感及长时跨环境验收仍未全部完成；清单父项保留未勾选。

以下章节保留历史批次记录，当前状态以本节和 repair-tasks.md 为准。

## 第一批：确定性错误

已实现并通过当前构建／相关回归：

- R025：笔记明确保存，独立 ModelContext 写入且返回失败；书签操作保留草稿，使用当前歌曲播放秒数或手动时间，拒绝非有限／负数／无法安全格式化的时间。曲目笔记表面提供当前歌曲的书签跳转。已有 timestampMs 字段继续按历史“秒”语义使用，未做单位迁移。
- R003：新旧缓存条目均按合法 ID 分类；频道和歌单不进入搜索导入和搜索集合队列。全局搜索频道显示专门入口，目前打开官方频道页面；完整原生频道搜索浏览仍属于 R009。
- R004：浅色 Now Playing 使用浅色艺术背景，保持深色正文和控制可读；迷你播放器标题改为语义色。
- R005：暂停的当前曲目不再显示 Playing；激活当前卡片通过 facade 切换播放，其他歌曲保留原集合播放。
- R007：没有当前时间行时，不施加逐行距离模糊或降低原文透明度。
- R026：桌面纯文本歌词显示准确的阅读引导，不再谎报无歌词。
- R008：AppKit 保留容器隐藏非活跃表面；表格仍常驻以保留状态，但不再泄漏隐藏的辅助功能行和排序按钮。
- R011：Chrome Cookie 状态、账号设置路径修正；旧测试中要求 Chrome 显示未连接的断言已更新。

实机验证：

- 保存临时笔记，关闭并重开，确认内容保留；随后清空并保存，恢复原来的空笔记。没有新增测试书签。
- 卡片模式只剩卡片 AX 子树；展开后显示完整表格，隐藏的重复操作消失。
- 暂停曲目的卡片 AX 标签为 Play，与 PlayerBar 一致。
- 浅色 Now Playing 的标题、歌词和 transport 可读：[截图](now-playing-light.jpg)。
- 真实 Bruno Mars 查询中首项明确为 Channel，不再是歌曲播放项：[截图](search-types.jpg)、[AX](search-types-ax.txt)。
- 当前覆盖仍不包括所有 macOS 版本、VoiceOver 朗读和系统辅助功能组合，这些由 R023 跟进。

## 第二批：图形与播放处理基础

已实现：

- R012：AudioInfoPanel 通过 SpectrumView 使用真实 GPU 偏好；Metal 不可用时回退 Canvas。该设置仍只管理频谱，未宣称它控制全局系统合成。
- R013：Metal teardown 改为 static，暂停 MTKView、解除 delegate 并释放 renderer；频谱处理器使用归属 token，旧表面销毁不会清除新处理器。Canvas 不再在 body 求值中写 peaks 状态，统一服从父视图尺寸。
- R020 部分：PerfTrace 限制保留最新 1024 条记录。音频分析能力跟随实际播放路径，条件性系统集成不再一律标为全支持；其他操作级探测尚未完成。
- R021 部分：引擎保留流式期间的 EQ 和频谱意图，在文件交接后应用；新增真实可观察处理状态，界面区分等待下载、可用、仅流式和未就绪。十段 EQ 尚未扩展成新的参数编辑产品。
- R010：Favorites、Music Videos、Subscriptions 加入原生导航菜单。
- R018 部分：加入 Dock 的 Play／Pause、Previous、Next，复用唯一 PlaybackService；关闭主窗口不终止应用。触觉及完整快捷键差距尚未完成。computer-use 读取 Dock 超时，因此 Dock 实机交互仍未验收。
- 音频信息和 EQ 面板提供明确的 Done 关闭按钮；Escape 原本即可关闭音频信息，现保留该行为。

验证：最新全量 **469 tests / 61 suites 通过**（`/tmp/muses-repair-stage2-tests.log`）。新增覆盖非视频拒绝、无效队列条目、笔记持久化及时间验证、旧频谱处理器退出、跟踪容量；已有混合流测试增加实际 EQ 增益恢复与能力状态断言。未把处理参数验证描述成已完成长时间听感／能耗评测。

## 尚待继续

R006 已完成入口接续的首个子项，完整会话协调仍待实施；R001 生产 Push、R002 身份修复未开启；R017 全局玻璃和动效、第四阶段的缺失功能、R023 全覆盖和 R024 清理仍待实施。部分完成项需要继续补齐其原验收门槛。

## 第三批：清单核实与视频入口接续

- 清单增加已完成／待验证子项：父项仍按完整验收门槛勾选，避免将代码落地和跨环境通过混为一谈。
- R006 部分：由 PlaybackService 在暂停原生音频之前提供不可变的 VideoPlaybackStart；包含当前秒数、播放意图和音量。非当前视频不继承旧进度或自动播放意图。
- WebKit 在原生暂停之后创建。iframe 默认 autoplay=0，提供 start 起点后备参数；依据 [YouTube IFrame API](https://developers.google.com/youtube/iframe_api_reference)，ready 后先设置音量，再按意图调用 loadVideoById 或 cueVideoById。切曲时关闭旧视频，避免使用旧起点重新加载新歌曲。
- R026 补充：同步前奏等待不再误标非同步；加载、无歌词、纯文本及同步当前行分别显示，切曲重新加载时清除旧内嵌偏移。新增状态与偏移测试。

验证与限制：

- 全量回归：472 tests / 62 suites 通过；应用构建、签名验证及启动通过。原测试要求 autoplay=1，与本次暂停保持行为冲突，已更新为 autoplay=0；HTML 注入防护断言保留，改为检查恶意 ID 是否进入页面。
- 实机从暂停的 1:04／3:31 打开视频，显示 Play video；没有自动开始。关闭后原生仍为暂停、1:04、52% 音量。最终构建再次验证 URL 含 start=63（真实位置约 63.8 秒，UI 四舍五入为 1:04）：[AX 记录](video-paused-start-ax.txt)、[截图](video-paused-start.png)。
- 手动开始视频时出现黑屏缓冲，字幕有内容但时间控件显示 0:00。**不判定视频进度或音量实机验收通过**；已补入 URL start 后备，完整端到端播放仍须复核。
- 本批未实现视频进度回传、系统媒体命令转发、错误／完成事件、基于 WebKit 停止确认的音频恢复。现有关闭延时不是“永不双声源”的证明；这些仍是 R006 必须完成的后续子项。
- 未修改生产 Push 开关、目录身份或持久化模型；未宣称完成 Liquid Glass／GPU 性能验收。

## 第六批最新进展

见 [第六批实施与验收](round-6.md)：窗口左上角原生同栏控件、真实前进／后退历史、外部 YouTube 播放链接、目录名称身份隔离、退役页面清理及发布资源包修补。486 项测试通过；实机工具栏、折叠和未解析目录提示已验证。完整修复清单尚未全部完成。

## 第五批：完整 Logo、居中视频与歌词时钟

- AppIcon 从完整原始 logo.png 构建，去掉原 icon.png 预绘制的圆角底板；开发启动脚本同步构建和复制 ICNS，避免运行旧资源。MusesMark 不再裁切标志。macOS 自己的图标处理与缓存未实机确认。
- 菜单栏改用独立简化的竖琴／M 模板，18pt 缓存，资产为 MenuBarMark.png；系统菜单栏实际显示尚未验收。
- 视频依据可用宽高缩放并居中，关闭在视频内右上，留出顶部控件区。实机 1229×768 窗口左右留白一致，内容区上下留白一致：[截图](video-centered.png)。保留背景与 Escape 关闭。
- 主歌词、全屏和桌面歌词读取活跃 transportState；Reduce Motion 只禁用滚动动画，不再停止歌词时钟。纯文本明确显示无时间轴提示。缓存可尝试升级为文字及录音匹配的供应商同步版本，旧请求不能覆盖新的手动选择。
- 实际 Risk It All 缓存仍为纯文本；同步候选的时长和文字有差异，未自动替换，未生成虚构时间。已观察到无时间轴提示和候选列表，这首歌逐行跟随仍未验收。Apple Intelligence 保留候选匹配及音译辅助；本轮没有新增 Siri 集成，也不声称 Siri 提供歌词时间轴。
- R006：iframe 音量／静音变化通过当前会话写回唯一音量偏好和原生引擎，不回声发送给 iframe；关闭／过期会话不能覆盖新会话。应用音量命令会解除 iframe 静音。新增有效值、静音、非法值、过期会话和无回声测试；双向音量实机仍待验收。
- 全量 **482 tests / 63 suites 通过**（`/tmp/muses-latest-repairs-tests.log`），包含缓存同步升级、短窗口视频尺寸及音量回传；没有以测试数代替跨环境或性能验收。
- 实机检查没有更换歌词或开始播放，退出候选后仍暂停在约 2:38／3:31，52% 音量。没有执行生产 Push、身份迁移或新增授权操作。

## 第四批：截图反馈与音视频活跃会话

本节是第三批之后的最新状态；前文的“未实现”保留为当时记录，以本节为准。

视觉修改：

- 视频去除外层玻璃框、圆角包裹和 Video 标题，保留独立关闭按钮、Escape 和背景关闭。实机：[无边框视频](video-borderless.png)。
- 侧栏移除顶部 Logo／字标；底部设置入口改为单色 Logo 图标。[侧栏与图标按钮](icon-sidebar.png)。设置分类也使用 Logo 标识，保留分类名称以辨识目的地：[设置 Logo](settings-logo.png)。
- 菜单栏状态图标改为现有 Logo 的透明单色模板，缓存 18pt／24pt 图像；修复 SwiftPM `.copy("Resources")` 下漏读 Logo 的问题。设置 About 复用统一 Logo。macOS 应用菜单命令保留文字。
- Home／搜索／歌单的播放、重试、搜索、新建，以及音频、歌词、导入面板关闭等常用动作使用图标和 help／AX 标签；设置 GitHub／检查更新／下载动作也改为图标。设置分类、说明和确认对话框保留必要文字。
- 已将此次明确视觉决定同步到 AGENTS.md，避免后续恢复旧侧栏字标或视频玻璃框。

R006 实施：

- VideoPlaybackSession 持有视频专属状态；PlaybackService 仍是唯一命令入口，transportState 供 NowPlayingManager、Dock 和菜单栏／迷你播放器读取。原生引擎状态独立，视频期间保持暂停。
- IFrame API 回传 ready、状态、进度、错误和自动播放受阻事件；仅接收主文档且会话 UUID 匹配的数据，拒绝非法时间及退役会话。命令经 JSON 序列化发送，ready 后使用最新播放意图；15 秒未 ready 显示不可用提示。
- 关闭取消轮询并等待 WebKit pauseAllMediaPlayback 的确认，销毁文档后释放音频暂停 token，替代固定 150ms 延时。只有当前会话、当前 load sequence 和相同媒体身份可以写回位置。
- 视频自然结束关闭视频并交回集合队列的完成路径；切歌先退役视频，旧会话结束回调只释放自己的 token。失败会话不自动恢复声音。
- R024：移除确认无引用的旧 YouTubeVideoWell，未删除正在使用的 SpectrumView。

实机和剩余边界：

- 暂停状态打开保持待播；手动视频播放从约 1:04 接续，在约 1:13 暂停后关闭，原生返回显示约 1:14 且仍暂停（视频和应用显示采用不同取整）。[返回状态 AX](video-session-return-ax.txt)。此前持续显示 0:00 的问题在本轮有效播放中未复现。
- 再次打开后使用应用命令 ⌘P 开始／暂停视频，视频到约 1:25 后显示 Play video，证明应用命令已进入视频入口。[命令暂停 AX](video-command-pause-ax.txt)。测试结束回退到约 1:05，并保持暂停、52% 音量。
- Control Center 的 computer-use 读取超时，未视为硬件媒体键或系统 UI 实测通过；MPRemoteCommand 的同一 facade 路由有代码和行为测试，但仍需要真实系统入口验收。
- IFrame 内音量变化回写原生偏好尚未实现；打开时音量和应用音量命令已接入。加载中关闭／快速切曲和完整队列完成仍需进一步跨子系统实机回归。因此 R006 父项继续未勾选。
- 不把本轮视觉截图与测试数当作全局 GPU／帧率、全部 Liquid Glass 或跨环境验收完成。

第四批最终验证：**479 tests / 63 suites 通过**（`/tmp/muses-visual-media-final-tests.log`），包含视频命令与原生静音隔离、过期会话、非法进度、失败后晚到 ready、自然结束队列只推进一次、视频暂停保存活跃位置。SessionService 的进度保存与睡眠状态读取活跃 transport；视频模式的 EQ／分析能力按实际 WebKit 路径显示不可用。图标按钮采用统一 28pt 最小标签点击区域，并保留原快捷键、帮助与辅助功能名称。最新应用构建、签名验证与启动通过；没有执行生产远端写入。

## 第七批：第一阶段专项修复

详见 [第一阶段专项修复与验收](phase-1-validation.md)。最新窗口要求已实现：侧栏玻璃延伸到红绿灯背后，视频关闭改为顶部中央触发弹出。完成 R025、R003、R005、R008，并补修歌词跨表面、浅色音量、手动歌词选择竞态和桌面歌词长行排版。494 项测试通过；实机验证只读笔记、输入空格、VoiceOver 隐藏表格、菜单／键盘播放、真实同步歌词与浅深色辅助功能外观。OAuth 未配置与 AI／翻译失败环境仍限制完整第一阶段验收，未把这些项目勾选为完成。

### 第七批收尾（2026-09-16）

- 第一阶段关闭 R025、R003、R004、R005、R007、R026、R008。R011 的真实 OAuth 条件仍缺失；不宣称第一阶段全部验收完成。
- 深色封面辅助功能／小窗口滚动、AI 与翻译故障注入、桌面双行歌词、时间偏移入口及未入库曲目偏移持久化完成。
- 视频 Tab 触发补修并实测，Return 关闭通过。495 tests / 65 suites 通过，应用重新构建并校验签名。详见 [专项验收](phase-1-validation.md)。


## 第九批：第三阶段（2026-09-16）

第二阶段 R006／R013／R021 的剩余硬件与长时验收按用户决定延期。第三阶段完成的代码和实机证据见 [专项记录](phase-3-validation.md)：集合返回／语言状态保持、命令 enabled 检查、局部玻璃、歌词菜单位置及卡片激活效果。实机进一步发现关窗后不能重建主窗口，已修复构建，等待旧进程退出后部署复测；没有将本阶段虚标完成。

## 第十批：设置整合与第四阶段首批（2026-09-16）

用户已退出旧进程。设置整合主窗口，分类改为语义图标；第三阶段剩余内容继续待办。修复搜索旧结果、取消、错误及重试状态，补上主窗口关闭时的搜索导航暂存。510 tests / 69 suites 通过，实机设置多语言／小窗口、搜索取消、File 主窗口重建、Search → Songs 重建及无效外部链接通过。见 [第四阶段首批记录](phase-4-validation.md)。第四阶段父项继续未勾选。

## 第十一批：第二至第四阶段续修

Settings 背景统一并实测浅深色；视频音量增加命令版本隔离及初始化静音同步；搜索增加加载更多及取消竞态保护；目录增加严格频道归属、错误反馈、限时缓存与强制刷新；新增 macOS 15+ App Intent 及元数据打包。513 tests / 69 suites 通过。R022 已形成可审阅架构比较，等待用户范围选择。见 [续修记录及逐项剩余内容](phase-2-through-4.md)。R014／R015 等未实现内容保持未完成，不等同于仅待验收。

本批追加：章节原生弹层首批接入，视频身份和时间过滤测试通过；真实无章节内容显示空状态。完整章节跳转验收与其余 R015 子项仍保留待办。最新全量回归 515 tests / 70 suites。

## 第十二批：设置标题与尺寸适配，第二至第四阶段续修

设置标题移出原生工具栏，子页返回在内容标题旁；展开／折叠侧栏均显示 Settings。可用内容宽度不足 640pt 使用分类菜单。实测 1280×800、1000×680、深／浅色、侧栏折叠及队列打开，截图见本目录 settings-layout-*。

R006 新增 seek 版本隔离，避免旧进度／结束包覆盖新跳转；R018 主窗口弱引用登记避免仅依赖 SwiftUI 可重写的字符串标识。R015 视频内补章节入口，与 Now Playing 复用查询和 PlaybackService。516 tests / 70 suites 通过，Debug 构建及开发包签名通过。

未将第二至第四阶段父项勾选：硬件／长时及实际有章节内容跳转等验收尚未完成；Smart Shuffle、完整播客及其余 YouTube 互动仍有真实开发工作，详见 repair-tasks.md。

窗口恢复检查曾出现两个浏览窗口，其中恢复窗口的标识变为 SwiftUI 值；已补弱引用识别回归，并确认后一次启动只有一个主窗口。未据一次启动把 Dock／恢复的全部矩阵标为通过。最终主窗口显式隐藏 SwiftUI 工具栏标题；队列组合截图保留了该最后修正前的 Muses 标题，但设置标题已位于内容区。视频显示时额外禁用并隐藏底层浏览内容的辅助功能入口，避免 Tab／AX 操作背景设置。

最终视频实机：Tab 显示关闭／章节控件，打开章节得到真实无章节提示；第一次 Escape 只关闭章节弹层，第二次关闭视频。底层设置不再出现在视频打开时的 AX 树；返回后仍暂停、5% 音量。Release 构建通过；最后的 AX 隔离改动通过 Debug 构建及上述实机验证。
