# Phase 11: Comprehensive UI Refinement + Feature Completion

## Overview
12 user-reported issues, organized into 10 implementation tasks:

---

## Task 1: Logo bundling fix (item 1)

**Files:** `Scripts/build-app.sh`, `Features/SidebarView.swift`, `Features/Settings/SettingsSheet.swift`, `Features/Settings/AboutView.swift`

**Root cause:** `build-app.sh` line 78 copies only `appcast.xml yt-dlp yt-dlp-LICENSE AppIcon.icns` — `logo.png` is missing from the list. `Bundle.main.url(forResource: "logo")` returns nil in the .app.

**Fix:**
- `build-app.sh`: Add `logo.png` to the copy list
- All 3 call sites: Add `Bundle.module` fallback (like YTDlpBridge does for yt-dlp)

---

## Task 2: PlayerBar — detail-column only, reasonable size (item 2)

**Files:** `App/RootView.swift`, `Features/PlayerBar.swift`

**Change:** Move PlayerBar from full-window `.overlay(alignment: .bottom)` to `.safeAreaInset(edge: .bottom)` on the detail Group — constrains it to the right detail column only.

- Remove the `.overlay(alignment: .bottom)` PlayerBar from RootView
- Add `.safeAreaInset(edge: .bottom, spacing: 8)` to the detail Group
- PlayerBar: reduce height 72→64, cornerRadius 20→16, shadow radius 12→8
- Remove the `.padding(.bottom, 96)` (safeAreaInset handles it)

---

## Task 3: Sidebar glass effect (item 3)

**Files:** `Features/SidebarView.swift`

**Fix:** Remove `.background(.ultraThinMaterial)` from VStack. Add `.containerBackground(.ultraThinMaterial, for: .navigation)` to the List (proper macOS 14+ API). Keep `.scrollContentBackground(.hidden)`.

---

## Task 4: Dark mode text contrast (item 5)

**Files:** `App/RootView.swift` (BrandColors)

- `textSecondary` dark: `rgb(0.53, 0.53, 0.57)` → `rgb(0.65, 0.65, 0.68)` (brighter)
- `surface` dark: `rgb(0.08, 0.08, 0.10)` → `rgb(0.15, 0.15, 0.17)` (visible cards)

---

## Task 5: Language settings + i18n override (item 6)

**Files:** `App/L10n.swift`, `Domain/UserPreferences.swift`, `App/RootView.swift`, new `Features/Settings/LanguageSettingsView.swift`

- `UserPreferences.swift`: Add `PrefKey.language` + `enum AppLanguage { system, en, zh }`
- `L10n.swift`: `isChinese` reads UserDefaults first, falls back to system locale
- `RootView.swift`: `@AppStorage(PrefKey.language)` + `.id(language)` to force re-render on change
- `LanguageSettingsView.swift`: radio group picker (System / English / 中文)

---

## Task 6: Settings restructure — left categories + right content (items 4, 7, 8)

**Files:** Rewrite `Features/Settings/SettingsSheet.swift`, modify settings sub-views

**New layout:** HStack with category List (180pt left) + detail (right).

Categories: General (GPU, notifications, language) / Playback (crossfade, replaygain, EQ) / Audio Quality (merged local+YT) / Library (scan roots) / Appearance (theme, now playing mode) / YouTube (cookie, yt-dlp) / Lyrics (source) / About (logo, version, GitHub link, update check)

**Audio Quality merge (item 4):** Split `PrefKey.audioQuality` into `localAudioQuality` + `ytAudioQuality`. One section with two pickers.

**Update → About (item 8):** Move update controls to About category. Add GitHub link button.

---

## Task 7: Search restructure + YouTube sidebar removal (item 9)

**Files:** `App/RootView.swift`, `Features/SidebarView.swift`, `Features/Search/GlobalSearchView.swift`

- Remove `youtubeMusic` from SidebarSection, add `youtubeImports` under Library
- GlobalSearchView: Add segmented Picker [Library] [YouTube Music] to filter results
- SidebarView: Add "YouTube Imports" item in Library section

---

## Task 8: YouTube album detail view (item 11)

**Files:** New `Features/YouTube/YouTubeAlbumDetailView.swift`

- Modern album/song list view for YouTube playlists
- Fetch playlist details via `YTDlpBridge.fetchPlaylist(url:)`
- Large artwork, title, channel, track list with play/import buttons
- Accessed from global search YouTube results

---

## Task 9: Home YouTube content + New local recommendations (items 10, 12)

**Files:** `Features/HomeView.swift`, `Features/NewView.swift`, new `Services/Recommendation/RecommendationService.swift`

**Home:** Add YouTube trending section (yt-dlp search "trending music") + imported playlists section.

**New:** Local recommendation algorithm — RecommendationService analyzes play history + liked tracks → recommended albums, "because you liked", discover unplayed.

---

## Task 10: Feature completion + regression (item 12)

**Files:** `Features/Settings/AudioQualitySettingsView.swift`, `Features/Settings/ThemeSettingsView.swift`, `Infrastructure/YTDlpBridge.swift`

- Wire YouTube audio quality to yt-dlp format selection
- Fix stale ThemeSettingsView caption
- Remove dead SettingsPlaceholderView
- Full test suite + make app + commit + push + memory

---

## Execution Order
Tasks 1-5 (quick fixes) → Task 6 (settings restructure) → Tasks 7-9 (features) → Task 10 (regression)